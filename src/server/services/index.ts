'use strict';

import plugin from "./plugin";

export default {
  plugin,
}

/*
const plugin = require('./plugin');
const googleGeocoder = require('./google-geocoder');

module.exports = {
  plugin,
  googleGeocoder,
}; */
